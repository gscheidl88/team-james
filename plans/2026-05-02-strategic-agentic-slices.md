# Strategic agentic slices — synthetic eval fixture

> This file is a committed fixture for routed delegation eval cases.

## Overview

This plan represents a routed implementation slice used by the CAO helper and session wrapper evals.

### Validation plan

- confirm routed metadata is emitted
- confirm preflight checks remain visible

### Replan rule

If routed delegation loses auditability, fall back to the explicit delegate path before expanding helper usage.

### Handoff state

- Current slice: runtime coverage
- Next slice: broader policy validation
